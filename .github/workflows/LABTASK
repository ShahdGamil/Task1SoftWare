name: Echo Hello World on Pull Request

on:
  pull_request:
    types: [opened]

jobs:
  hello-world-job:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Hello World
        run: echo "Hello, World!"
